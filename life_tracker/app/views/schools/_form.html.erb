<%= form_for(@school) do |f| %>
  <% if @school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% @school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form">
  <form>
    <div class="row">
      <div class="small-4 columns">
        <label>Person
          <div class="field">
    <%= f.collection_select :person_id, Person.all, :id, :name, :include_blank => true %>
  </label>
        </div>
      </div>
    </div>

  <div class="row">
      <div class="small-4 columns">
        <label>Name
          <div class="field">
            <%= f.text_field :name %>
          </div>
        </label>
      </div>
    </div>

  <div class="row">
      <div class="small-4 columns">
        <label>Beginning Year
          <div class="field">
            <%= f.text_field :beginningYear %>
          </div>
        </label>
      </div>
    </div>

  <div class="row">
      <div class="small-4 columns">
        <label>Ending Year
          <div class="Description">
            <%= f.text_field :endingYear %>
          </div>
        </label>
      </div>
    </div>
</form>
</div>

<div class="actions">
  <%= f.submit %>
</div>

<% end %>
