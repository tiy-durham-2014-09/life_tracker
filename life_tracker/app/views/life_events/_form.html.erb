<%= form_for(@life_event) do |f| %>
<% if @life_event.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@life_event.errors.count, "error") %> prohibited this life_event from being saved:</h2>

  <ul>
    <% @life_event.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :person %><br>
  <%= f.collection_select :person_id, Person.all, :id, :name, :include_blank => true %>
</div>

<div class="field">
  <%= f.label :event %><br>
  <%= f.text_field :event %>
</div>
<div class="field">
  <%= f.label :date %><br>
  <%= f.date_select :date, :start_year => 1900, :end_year => 2014 %>
</div>
<div class="field">
  <%= f.label :description %><br>
  <%= f.text_field :description, :size => 50, :maxlength => 140 %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
