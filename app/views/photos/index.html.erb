<div class="row">
  <ul class="small-block-grid-3">
    <% @photos.each do |photo| %>
      	  <li><%= link_to image_tag(photo.image.url(:thumb),:class => "thumbs"), photo %></li>
    <% end %>
  </ul>  
</div>