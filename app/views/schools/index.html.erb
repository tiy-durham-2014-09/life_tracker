<h1>These are your schools</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>

  <% @schools.each do |school| %>
    <tr>
      <td><%= school.title %></td>
      <td><%= school.start_year %></td>
      <td><%= school.end_year %></td>
    </tr>
  <% end %>
</table>
